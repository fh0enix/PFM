<%= form_with(model: operation) do |form| %>
  <% if operation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(operation.errors.count, "error") %> prohibited this operation from being saved:</h2>

      <ul>
        <% operation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= select(:operation, :category_id, @category_options) %>
  </div>

  <div>
    <%= label :spend_earn, "Витрата", value: false %>
    <%= form.radio_button :spend_earn, false, :checked => true, :value => false %>
  </div>

    <div>
    <%= label :spend_earn, "Дохід", value: true %>
    <%= form.radio_button :spend_earn, true, :value => true %>
  </div>

  <div>
    <%= form.label :sum, style: "display: block" %>
    <%= form.text_field :sum %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
