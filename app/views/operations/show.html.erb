<p style="color: green"><%= notice %></p>

<%= render @operation %>

<div>
  <%= link_to "редагувати транзакцію", edit_operation_path(@operation) %> |
  <%= link_to "повернутись до транзакцій", operations_path %>

  <%= button_to "Destroy this operation", @operation, method: :delete %>
</div>
